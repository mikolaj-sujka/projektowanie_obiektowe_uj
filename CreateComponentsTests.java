import edu.uj.po.simulation.interfaces.UnknownChip;
import org.junit.Test;

import static org.junit.Assert.*;

public class CreateComponentsTests {

    @Test
    public void testCreateComponent7400() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(7400);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent7402() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(7402);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent7404() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(7404);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent7408() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(7408);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent7410() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(7410);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent7411() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(7411);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent7420() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(7420);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent7431() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(7431);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent7432() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(7432);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent7434() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(7434);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent7442() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(7442);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent7444() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(7444);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent7482() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(7482);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent74138() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(74138);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }

    @Test
    public void testCreateComponent74152() throws UnknownChip {
        Simulation simulation = new Simulation();

        int componentId = simulation.createChip(74152);

        // Check that the component was created with a valid ID
        assertTrue(componentId > 0);
    }


}
